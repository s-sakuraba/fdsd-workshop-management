@{
    ViewData["Title"] = "Create";
    var gakkaList = (IEnumerable<Fdsd.Application.Common.GakkaDto>)ViewBag.GakkaList;
}
<h2>所属登録</h2>
<a href="@Url.Action("Index")" class="btn btn-sm btn-secondary mb-3">戻る</a>
<hr />
@Html.ValidationSummary(true, "", new { @class = "text-danger" })

<form asp-action="Create" method="post">
    <input type="hidden" name="userId" value="@ViewBag.UserId" />
    <div class="form-horizontal">
        <div class="form-group">
            <label class="control-label col-md-2">異動対象者</label>
            <div class="col-md-10">
                <p class="form-control-plaintext">@ViewBag.UserName</p>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">異動先学科</label>
            <div class="col-md-10">
                <select name="gakkaCd" class="form-select" style="max-width:280px;" required>
                    <option value="">-- 選択してください --</option>
                    @foreach (var g in gakkaList)
                    {
                        <option value="@g.GakkaCd">@g.GakkaName</option>
                    }
                </select>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">所属開始日</label>
            <div class="col-md-10">
                <input type="date" name="dateOfArrival" value="@DateTime.Now.ToString("yyyy-MM-dd")" class="form-control" style="max-width:280px;" required />
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">所属終了日</label>
            <div class="col-md-10">
                <input type="date" name="dateOfDeparture" value="9999-12-31" class="form-control" style="max-width:280px;" required />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-outline-dark">登録</button>
            </div>
        </div>
    </div>
</form>

<div>
    <a href="@Url.Action("Index")" class="btn btn-sm btn-secondary">戻る</a>
</div>