@{
    ViewData["Title"] = "Create";
    var userList = (IEnumerable<Fdsd.Application.Common.UserDto>)ViewBag.UserList;
}
<h3>休職登録</h3>
<a href="@Url.Action("Index")" class="btn btn-sm btn-secondary mb-3">戻る</a>
<hr />
@Html.ValidationSummary(true, "", new { @class = "text-danger" })

<form asp-action="Create" method="post">
    <div class="form-horizontal">
        <div class="form-group">
            <label class="control-label col-md-2">教職員名</label>
            <div class="col-md-10">
                <select name="userId" class="form-select" style="max-width:280px;" required>
                    <option value="">-- 選択してください --</option>
                    @foreach (var u in userList)
                    {
                        <option value="@u.UserId">@u.EmpName</option>
                    }
                </select>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">休職開始日</label>
            <div class="col-md-10">
                <input type="date" name="startDate" value="" class="form-control" style="max-width:280px;" required />
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">休職終了日</label>
            <div class="col-md-10">
                <input type="date" name="endDate" value="" class="form-control" style="max-width:280px;" />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-outline-dark">登録</button>
            </div>
        </div>
    </div>
</form>

<div>
    <a href="@Url.Action("Index")" class="btn btn-sm btn-secondary">戻る</a>
</div>